# Iris-Classification-DT-RF-SVM
Comparing Decision Tree, Random Forest, and SVM classifiers on the Iris dataset, with EDA, hyperparameter tuning via RandomizedSearchCV, and train/test accuracy benchmarking.
